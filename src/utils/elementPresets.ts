/**
 * 预设元素 (兼容导出)
 * 
 * @deprecated 请使用 './presets' 导入
 */
export { 
  ELEMENT_PRESETS, 
  PRESETS_BY_CATEGORY,
  CATEGORY_LABELS,
  findPresetById,
  getPresetsByCategory,
  type ElementPreset,
  type PresetCategory,
  type PresetsByCategory,
} from './presets';
